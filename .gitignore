.DS_Store
/.build
/Packages
/*.xcodeproj
xcuserdata/
.swiftpm/
lib/
bin/
libssh2/
openssl-frameworks/
*.zip
openssl/
frameworks/
xcframeworks/
apple.cmake
release.md
# Written into this tree by the consuming repo's Scripts/build-libssh2.sh, which
# uses a clone of this fork as its build directory. Untracked and unignored, it
# was one `git add -A` away from landing here.
build.log
